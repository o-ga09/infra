apiVersion: v1
kind: Service
metadata:
  name: line-bot-webhook
  namespace: line-bot
spec:
  selector:
    app: line-bot-webhook
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  type: ClusterIP
