apiVersion: batch/v1
kind: Job
metadata:
  name: line-bot-migration
  namespace: line-bot
  annotations:
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      imagePullSecrets:
      - name: gar-secret
      containers:
      - name: migration
        image: asia-northeast1-docker.pkg.dev/mh-api-389212/ai-kanji/webhook:82b8ceed720253cb5acd37b0f4a85fe3fd3b7b02
        command: ["./migrate"]
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: line-bot-secret
              key: DATABASE_URL
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "200m"
