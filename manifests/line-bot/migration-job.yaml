apiVersion: batch/v1
kind: Job
metadata:
  name: line-bot-migration
  namespace: line-bot
  annotations:
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      imagePullSecrets:
      - name: gar-secret
      containers:
      - name: migration
        image: asia-northeast1-docker.pkg.dev/mh-api-389212/ai-kanji/webhook:c1dce3b801b2271b83eff5b6fc1a14cfd542161c
        command: ["./migrate", "-command", "up"]
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: line-bot-secret
              key: DATABASE_URL
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "200m"
