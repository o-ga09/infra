apiVersion: apps/v1
kind: Deployment
metadata:
  name: line-bot-webhook
  namespace: line-bot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: line-bot-webhook
  template:
    metadata:
      labels:
        app: line-bot-webhook
    spec:
      imagePullSecrets:
      - name: gar-secret
      containers:
      - name: line-bot-webhook
        image: asia-northeast1-docker.pkg.dev/mh-api-389212/ai-kanji/webhook:b383b0072dd9979328d9cf81e9854cc0cba37c3f
        ports:
        - containerPort: 8080
        env:
        - name: PORT
          value: "8080"
        - name: LINE_CHANNEL_SECRET
          valueFrom:
            secretKeyRef:
              name: line-bot-secret
              key: LINE_CHANNEL_SECRET
        - name: LINE_CHANNEL_ACCESS_TOKEN
          valueFrom:
            secretKeyRef:
              name: line-bot-secret
              key: LINE_CHANNEL_ACCESS_TOKEN
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: line-bot-secret
              key: DATABASE_URL
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: line-bot-secret
              key: REDIS_URL
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: line-bot-worker
  namespace: line-bot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: line-bot-worker
  template:
    metadata:
      labels:
        app: line-bot-worker
    spec:
      imagePullSecrets:
      - name: gar-secret
      containers:
      - name: line-bot-worker
        image: asia-northeast1-docker.pkg.dev/mh-api-389212/ai-kanji/worker:b383b0072dd9979328d9cf81e9854cc0cba37c3f
        env:
        - name: LINE_CHANNEL_SECRET
          valueFrom:
            secretKeyRef:
              name: line-bot-secret
              key: LINE_CHANNEL_SECRET
        - name: LINE_CHANNEL_ACCESS_TOKEN
          valueFrom:
            secretKeyRef:
              name: line-bot-secret
              key: LINE_CHANNEL_ACCESS_TOKEN
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: line-bot-secret
              key: DATABASE_URL
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: line-bot-secret
              key: REDIS_URL
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
