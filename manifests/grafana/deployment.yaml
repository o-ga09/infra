apiVersion: apps/v1
kind: Deployment
metadata:
  name: kube-prometheus-stack-grafana
  namespace: monitoring
spec:
  template:
    spec:
      containers:
      - name: grafana
        envFrom:
        - secretRef:
            name: grafana-google-oauth
