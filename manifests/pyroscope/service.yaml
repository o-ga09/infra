apiVersion: v1
kind: Service
metadata:
  name: pyroscope
  namespace: monitoring
  labels:
    app: pyroscope
spec:
  selector:
    app: pyroscope
  ports:
  - port: 4040
    targetPort: http
    name: http
